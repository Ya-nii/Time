page {
  background: #F2F9F6; /* 统一浅绿背景 */
}
.page {
  padding: 20rpx;
}

/* 头部 */
.header {
  display: flex;
  align-items: center;
  padding: 30rpx;
  background: #fff;
  border-radius: 24rpx;
  margin-bottom: 30rpx;
}
.avatar {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  object-fit: cover;
  background: #E2F5EE;
}
.user-info {
  flex: 1;
  margin-left: 24rpx;
}
.nickname {
  display: block;
  font-size: 34rpx;
  font-weight: bold;
  margin-bottom: 8rpx;
  color: #2A5548;
}
.tag {
  font-size: 24rpx;
  color: #3CB997;
  background: #E6F7F3;
  padding: 4rpx 14rpx;
  border-radius: 20rpx;
}
.info-link {
  font-size: 26rpx;
  color: #6B8A82;
}

/* 图标快捷栏 */
.icon-bar {
  display: flex;
  justify-content: space-around;
  background: #fff;
  border-radius: 24rpx;
  padding: 30rpx 0;
  margin-bottom: 30rpx;
}
.icon-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}
.icon {
  width: 70rpx;
  height: 70rpx;
  background: #f7f8fa;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  margin-bottom: 10rpx;
  /* 图标颜色不改动 */
}
.label {
  font-size: 24rpx;
  color: #666;
}

/* 权益卡片 */
.benefit-card {
  background: #fff;
  border-radius: 24rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
}
.section-title {
  font-size: 30rpx;
  font-weight: bold;
  margin-bottom: 20rpx;
  color: #2A5548;
}
.benefit-row {
  display: flex;
}
.benefit-item {
  text-align: center;
}
.num {
  font-size: 44rpx;
  font-weight: bold;
  color: #3CB997;
}
.name {
  font-size: 26rpx;
  color: #666;
}

/* 列表卡片 */
.list-card {
  background: #fff;
  border-radius: 24rpx;
  overflow: hidden;
}
.list-item {
  display: flex;
  align-items: center;
  padding: 30rpx;
  border-bottom: 1rpx solid #f5f5f5;
}
.list-item:last-child {
  border-bottom: none;
}
.item-icon {
  font-size: 34rpx;
  margin-right: 20rpx;
  /* 图标颜色完全不改动 */
}
.item-text {
  flex: 1;
  font-size: 29rpx;
  color: #2A5548;
}
.tag-text {
  font-size: 24rpx;
  color: #3CB997;
  margin-right: 10rpx;
}
.arrow {
  font-size: 28rpx;
  color: #ccc;
}